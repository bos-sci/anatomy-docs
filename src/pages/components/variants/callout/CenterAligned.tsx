import { Callout } from '@boston-scientific/anatomy-react';
import Example from 'shared/components/Example';

const CenterAligned = (): JSX.Element => {
  return (
    <Example>
      <Callout isCentered>Center aligned callout</Callout>
    </Example>
  );
};

export default CenterAligned;
