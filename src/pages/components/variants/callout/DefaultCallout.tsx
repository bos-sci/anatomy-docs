import { Callout } from '@boston-scientific/anatomy-react';
import Example from 'shared/components/Example';

const DefaultCallout = (): JSX.Element => {
  return (
    <Example>
      <Callout>Default callout</Callout>
    </Example>
  );
};

export default DefaultCallout;
