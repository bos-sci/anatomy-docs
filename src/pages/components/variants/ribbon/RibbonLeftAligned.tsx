import { Ribbon } from '@boston-scientific/anatomy-react';
import Example from 'shared/components/Example';

const RibbonLeftAligned = (): JSX.Element => {
  return (
    <Example>
      <Ribbon textAlign="left">Left aligned Ribbon</Ribbon>
    </Example>
  );
};

export default RibbonLeftAligned;
