import { Button } from '@boston-scientific/anatomy-react';
import Example from 'shared/components/Example';

const SubtleNavBack = (): JSX.Element => {
  return (
    <Example>
      <Button type="button" className="bsds-button-nav-back bsds-body-subtle">
        Back
      </Button>
    </Example>
  );
};

export default SubtleNavBack;
