query getContentGuideline($id: String!, $preview: Boolean) {
  contentGuideline(id: $id, preview: $preview) {
    name
    leadParagraph
    content
    pageProperties {
      seoMetaDescription
    }
    sys {
      id
      publishedAt
    }
  }
}