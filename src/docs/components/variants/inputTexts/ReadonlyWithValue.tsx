import InputText from '../../../../library/components/InputText';

const ReadOnlyWithValue = (): JSX.Element => {
  return <InputText label="Read only input" value="Input value" readOnly />;
}

export default ReadOnlyWithValue;