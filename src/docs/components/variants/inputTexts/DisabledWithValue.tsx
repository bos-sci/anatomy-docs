import InputText from '../../../../library/components/InputText';

const DisabledWithValue = (): JSX.Element => {
  return <InputText label="Disabled input" value="Input value" disabled />;
}

export default DisabledWithValue;