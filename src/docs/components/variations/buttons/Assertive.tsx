import Button from '../../../../library/components/Button';

const Assertive = () => {
  return <Button variant="assertive" aria-label="Testing the aria label" type="button">Assertive Button</Button>;
}

export default Assertive;