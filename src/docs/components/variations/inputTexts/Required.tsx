import InputText from '../../../../library/components/InputText';

const Required = () => {
  return <InputText label="Required input" placeholder="Placeholder text" required />;
}

export default Required;