query getFoundation($id: String!, $preview: Boolean) {
  foundation(id: $id, preview: $preview) {
    name
    leadParagraph
    content
    pageProperties {
      seoMetaDescription
    }
    sys {
      id
      publishedAt
    }
  }
}
