query getFoundation($id: String!, $preview: Boolean) {
  foundation(id: $id, preview: $preview) {
    name
    leadParagraph
    content
    pageProperties {
      seoMetaDescription
    }
    usage
    usageDo
    usageDont
    secondaryContent
    sys {
      id
      publishedAt
    }
  }
}