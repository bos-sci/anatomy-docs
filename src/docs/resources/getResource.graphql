query getResource($id: String!, $preview: Boolean) {
  resource(id: $id, preview: $preview) {
    name
    leadParagraph
    content
    pageProperties {
      seoMetaDescription
    }
    sys {
      id
      publishedAt
    }
  }
}